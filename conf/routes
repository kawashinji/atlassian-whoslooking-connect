# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~



# Home page
GET     /                          controllers.Application.index()
GET     /viewables/:hostid/:resourceid/viewers     controllers.Viewers.get(hostid, resourceid)
PUT     /viewables/:hostid/:resourceid/viewers     controllers.Viewers.put(hostid, resourceid)
DELETE  /viewables/:hostid/:resourceid/viewers/:userid     controllers.Viewers.delete(hostid, resourceid, userid)
GET     /viewables                 controllers.Viewers.list()
GET     /poller					   controllers.Poller.index()


# Map static resources from the /public folder to the /assets URL path
GET     /myassets/*file               controllers.Assets.at(path="/public", file)

->      /                                   ac.Routes